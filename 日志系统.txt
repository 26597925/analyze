1.多线程分块读取数据
2.ip time way url status size, refer
3.通过ip生成一个数据结构：区域，时间段，PV, UV, 运营商，次数
  通过url生成一个数据结构：访问次数，是否有暴力破解
  通过user_agent生成一个数据结构：谁访问（百度，谷歌，正常访问），访问次数，什么方式访问（手机，其他）



第一步：
定义接口
第二部：
多文件多进程，分块分线程读取文件，异步返回数据。
第三部：
根据返回数据，封装指定数据结构保存。